<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stylefeng.guns.rest.persistence.dao.MtimeFieldTMapper">

    <!-- 通用查询映射结果
    <resultMap id="BaseResultMap" type="responseOrderVO">
        <id column="UUID" property="uuid" />
        <result column="cinema_name" property="cinemaName" />
        <result column="film_name" property="filmName" />
        <result column="begin_time" property="fieldTime" />
        <result column="end_time" property="endTime" />
        <result column="hall_id" property="hallId" />
        <result column="hall_name" property="hallName" />
        <result column="price" property="price" />
    </resultMap>
    <select id="searchByFiledId" resultMap="BaseResultMap">
        select
        mft.UUID,cinema_name,film_name,begin_time,end_time,hall_id,hall_name,price
        from
        filmplatform_cinema.mtime_field_t mft,
        filmplatform_cinema.mtime_cinema_t mct,
        filmplatform_cinema.mtime_hall_film_info_t mhfit
        where
        mft.UUID=#{filedId} and
        mct.UUID = cinema_id and
        mhfit.UUID = film_id

    </select>

    <select id="selectPrice" resultType="double">
        select
        price
        from
        filmplatform_cinema.mtime_field_t
        where
        UUID=#{filedId}
    </select>-->

</mapper>
